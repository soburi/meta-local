From 00f6027fb63e294b536302103c3f2989d0089929 Mon Sep 17 00:00:00 2001
From: TOKITA Hiroshi <tokita.hiroshi@gmail.com>
Date: Tue, 13 May 2025 22:09:32 +0900
Subject: [PATCH] debug printk

Upstream-Status: Submitted [tokita.hiroshi@gmail.com]
---
 tools/libs/light/libxl_dm.c | 19 +++++++-
 xen/arch/arm/dm.c           | 14 +++++-
 xen/common/event_channel.c  |  8 ++++
 xen/common/ioreq.c          | 91 +++++++++++++++++++++++++++++++------
 xen/common/memory.c         | 48 +++++++++++++++++++
 xen/include/xsm/xsm.h       |  1 +
 xen/xsm/xsm_core.c          |  7 +++
 7 files changed, 172 insertions(+), 16 deletions(-)

diff --git a/tools/libs/light/libxl_dm.c b/tools/libs/light/libxl_dm.c
index b193a5dc37..3ff5c7e460 100644
--- a/tools/libs/light/libxl_dm.c
+++ b/tools/libs/light/libxl_dm.c
@@ -915,7 +915,13 @@ static int libxl__build_device_model_args_old(libxl__gc *gc,
     flexarray_append(dm_args, "-M");
     switch (b_info->type) {
     case LIBXL_DOMAIN_TYPE_PVH:
+        LOG(WARN, "LIBXL_DOMAIN_TYPE_PVH");
+        flexarray_append(dm_args, "xenpvh");
+        for (i = 0; b_info->extra_pv && b_info->extra_pv[i] != NULL; i++)
+            flexarray_append(dm_args, b_info->extra_pv[i]);
+        break;
     case LIBXL_DOMAIN_TYPE_PV:
+        LOG(WARN, "LIBXL_DOMAIN_TYPE_PV");
         flexarray_append(dm_args, "xenpv");
         for (i = 0; b_info->extra_pv && b_info->extra_pv[i] != NULL; i++)
             flexarray_append(dm_args, b_info->extra_pv[i]);
@@ -1791,7 +1797,13 @@ static int libxl__build_device_model_args_new(libxl__gc *gc,
     flexarray_append(dm_args, "-machine");
     switch (b_info->type) {
     case LIBXL_DOMAIN_TYPE_PVH:
+        LOG(WARN, "LIBXL_DOMAIN_TYPE_PVH");
+        flexarray_append(dm_args, "xenpvh");
+        for (i = 0; b_info->extra_pv && b_info->extra_pv[i] != NULL; i++)
+            flexarray_append(dm_args, b_info->extra_pv[i]);
+        break;
     case LIBXL_DOMAIN_TYPE_PV:
+        LOG(WARN, "LIBXL_DOMAIN_TYPE_PVH");
         flexarray_append(dm_args, "xenpv");
         for (i = 0; b_info->extra_pv && b_info->extra_pv[i] != NULL; i++)
             flexarray_append(dm_args, b_info->extra_pv[i]);
@@ -3631,7 +3643,12 @@ void libxl__spawn_qemu_xenpv_backend(libxl__egc *egc,
     flexarray_vappend(dm_args, "-name",
                       GCSPRINTF("domain-%u", domid), NULL);
     flexarray_append(dm_args, "-nographic");
-    flexarray_vappend(dm_args, "-M", "xenpv", NULL);
+    if (dmss->domain_type == LIBXL_DOMAIN_TYPE_PVH)
+	    LOG(WARN, "LIBXL_DOMAIN_TYPE_PVH");
+	    flexarray_vappend(dm_args, "-M", "xenpvh", NULL);
+    else
+	    LOG(WARN, "LIBXL_DOMAIN_TYPE_PV");
+	    flexarray_vappend(dm_args, "-M", "xenpv", NULL);
     flexarray_vappend(dm_args, "-monitor", "/dev/null", NULL);
     flexarray_vappend(dm_args, "-serial", "/dev/null", NULL);
     flexarray_vappend(dm_args, "-parallel", "/dev/null", NULL);
diff --git a/xen/arch/arm/dm.c b/xen/arch/arm/dm.c
index 773a0a2592..18fca5f611 100644
--- a/xen/arch/arm/dm.c
+++ b/xen/arch/arm/dm.c
@@ -30,13 +30,19 @@ int dm_op(const struct dmop_args *op_args)
         [XEN_DMOP_nr_vcpus]                         = sizeof(struct xen_dm_op_nr_vcpus),
     };
 
+    printk("debug dm_op\n");
+
     rc = rcu_lock_remote_domain_by_id(op_args->domid, &d);
-    if ( rc )
+    if ( rc ) {
+	    printk("rcu_lock_remote_domain_by_id %ld\n", rc);
         return rc;
+    }
 
     rc = xsm_dm_op(XSM_DM_PRIV, d);
-    if ( rc )
+    if ( rc ) {
+	    printk("xsm_dm_op %ld\n", rc);
         goto out;
+    }
 
     offset = offsetof(struct xen_dm_op, u);
 
@@ -123,7 +129,9 @@ int dm_op(const struct dmop_args *op_args)
     }
 
     default:
+	    printk("ioreq_server_dm_op\n");
         rc = ioreq_server_dm_op(&op, d, &const_op);
+	    printk("ioreq_server_dm_op %ld\n", rc);
         break;
     }
 
@@ -135,6 +143,8 @@ int dm_op(const struct dmop_args *op_args)
  out:
     rcu_unlock_domain(d);
 
+    printk("return %ld\n", rc);
+
     return rc;
 }
 
diff --git a/xen/common/event_channel.c b/xen/common/event_channel.c
index 46281b16ce..a70017a70f 100644
--- a/xen/common/event_channel.c
+++ b/xen/common/event_channel.c
@@ -382,6 +382,8 @@ int evtchn_bind_interdomain(evtchn_bind_interdomain_t *bind, struct domain *ld,
     evtchn_port_t  rport = bind->remote_port;
     domid_t        rdom = bind->remote_dom;
 
+    printk("evtchn_bind_interdomain rp=%d rd=%d lp=%d\n", rport, rdom, lport);
+
     if ( (rd = rcu_lock_domain_by_any_id(rdom)) == NULL )
         return -ESRCH;
 
@@ -399,6 +401,7 @@ int evtchn_bind_interdomain(evtchn_bind_interdomain_t *bind, struct domain *ld,
     }
 
     lport = rc = evtchn_get_port(ld, lport);
+    printk("evtchn_get_port %d\n", lport);
     if ( rc < 0 )
     {
         gdprintk(XENLOG_WARNING, "EVTCHNOP failure: error %d\n", rc);
@@ -410,6 +413,7 @@ int evtchn_bind_interdomain(evtchn_bind_interdomain_t *bind, struct domain *ld,
     lchn = evtchn_from_port(ld, lport);
 
     rchn = _evtchn_from_port(rd, rport);
+    printk("rchn %p\n", rchn);
     if ( !rchn )
     {
         rc = -EINVAL;
@@ -420,12 +424,15 @@ int evtchn_bind_interdomain(evtchn_bind_interdomain_t *bind, struct domain *ld,
     if ( (rchn->state != ECS_UNBOUND) ||
          (rchn->u.unbound.remote_domid != ld->domain_id) )
     {
+    printk("rchn->state %d\n", rchn->state);
+    printk("remote_domid %d local_domid %d\n", rchn->u.unbound.remote_domid, ld->domain_id);
         rc = -EINVAL;
         gdprintk(XENLOG_WARNING, "EVTCHNOP failure: %pd, error %d\n", rd, rc);
         goto out;
     }
 
     rc = xsm_evtchn_interdomain(XSM_HOOK, ld, lchn, rd, rchn);
+    printk("xsm_evtchn_interdomain %d\n", rc);
     if ( rc )
         goto out;
 
@@ -458,6 +465,7 @@ int evtchn_bind_interdomain(evtchn_bind_interdomain_t *bind, struct domain *ld,
     
     rcu_unlock_domain(rd);
 
+    printk("evtchn_bind_interdomain end %d\n", rc);
     return rc;
 }
 
diff --git a/xen/common/ioreq.c b/xen/common/ioreq.c
index f5fd30ce12..88ba617daa 100644
--- a/xen/common/ioreq.c
+++ b/xen/common/ioreq.c
@@ -37,6 +37,8 @@ void ioreq_request_mapcache_invalidate(const struct domain *d)
 {
     struct vcpu *v = current;
 
+    printk("%s\n", __func__);
+
     if ( d == v->domain )
         v->mapcache_invalidate = true;
     else if ( d->creation_finished )
@@ -157,6 +159,8 @@ static bool wait_for_io(struct ioreq_vcpu *sv, ioreq_t *p)
     unsigned int state = p->state;
     uint64_t data = ~0;
 
+    printk("%s\n", __func__);
+
     smp_rmb();
 
     /*
@@ -263,6 +267,8 @@ static int ioreq_server_alloc_mfn(struct ioreq_server *s, bool buf)
     struct ioreq_page *iorp = buf ? &s->bufioreq : &s->ioreq;
     struct page_info *page;
 
+    printk("%s\n", __func__);
+
     if ( iorp->page )
     {
         /*
@@ -270,8 +276,10 @@ static int ioreq_server_alloc_mfn(struct ioreq_server *s, bool buf)
          * on demand if ioreq_server_get_info() is called), then
          * allocating a page is not permitted.
          */
-        if ( !gfn_eq(iorp->gfn, INVALID_GFN) )
+        if ( !gfn_eq(iorp->gfn, INVALID_GFN) ) {
+		printk("gfn_eq\n");
             return -EPERM;
+	}
 
         return 0;
     }
@@ -281,6 +289,13 @@ static int ioreq_server_alloc_mfn(struct ioreq_server *s, bool buf)
     if ( !page )
         return -ENOMEM;
 
+    /* Log the MFN we just grabbed */
+    {
+        mfn_t mfn = page_to_mfn(page);
+        printk("HV_DBG: ioreq_server_alloc_mfn page=%p mfn=0x%lx\n",
+                   page, (unsigned long)mfn);
+    }
+
     if ( !get_page_and_type(page, s->target, PGT_writable_page) )
     {
         /*
@@ -329,6 +344,8 @@ bool is_ioreq_server_page(struct domain *d, const struct page_info *page)
     unsigned int id;
     bool found = false;
 
+    printk("%s\n", __func__);
+
     rspin_lock(&d->ioreq_server.lock);
 
     FOR_EACH_IOREQ_SERVER(d, id, s)
@@ -348,12 +365,21 @@ bool is_ioreq_server_page(struct domain *d, const struct page_info *page)
 static void ioreq_server_update_evtchn(struct ioreq_server *s,
                                        struct ioreq_vcpu *sv)
 {
+    printk("%s\n", __func__);
     ASSERT(spin_is_locked(&s->lock));
+    printk("%s - \n", __func__);
 
     if ( s->ioreq.va != NULL )
     {
         ioreq_t *p = get_ioreq(s, sv->vcpu);
 
+        /* Log the same MFN as seen through s->ioreq.page */
+        {
+            mfn_t ring_mfn = page_to_mfn(s->ioreq.page);
+            printk("HV_DBG: update_evtchn p=%p ring_mfn=0x%lx vp_eport=%d\n",
+                   p, (unsigned long)ring_mfn, sv->ioreq_evtchn);
+        }
+
         p->vp_eport = sv->ioreq_evtchn;
     }
 }
@@ -366,6 +392,8 @@ static int ioreq_server_add_vcpu(struct ioreq_server *s,
 
     sv = xzalloc(struct ioreq_vcpu);
 
+    printk("%s\n", __func__);
+
     rc = -ENOMEM;
     if ( !sv )
         goto fail1;
@@ -647,6 +675,8 @@ static int ioreq_server_create(struct domain *d, int bufioreq_handling,
     unsigned int i;
     int rc;
 
+    printk("ioreq_server_create\n");
+
     if ( !IS_ENABLED(CONFIG_X86) && bufioreq_handling )
         return -EINVAL;
 
@@ -679,6 +709,7 @@ static int ioreq_server_create(struct domain *d, int bufioreq_handling,
     rc = ioreq_server_init(s, d, bufioreq_handling, i);
     if ( rc )
     {
+	    printk("ioreq_server_init %d\n", rc);
         set_ioreq_server(d, i, NULL);
         goto fail;
     }
@@ -751,21 +782,29 @@ static int ioreq_server_get_info(struct domain *d, ioservid_t id,
 
     rspin_lock(&d->ioreq_server.lock);
 
+	printk("ioreq_server_get_info\n");
+
     s = get_ioreq_server(d, id);
 
     rc = -ENOENT;
-    if ( !s )
+    if ( !s ) {
+	printk("!s\n");
         goto out;
+    }
 
     rc = -EPERM;
-    if ( s->emulator != current->domain )
+    if ( s->emulator != current->domain ) {
+	printk("s->emulator != current->domain \n");
         goto out;
+    }
 
     if ( ioreq_gfn || bufioreq_gfn )
     {
         rc = arch_ioreq_server_map_pages(s);
-        if ( rc )
+        if ( rc ) {
+		printk("arch_ioreq_server_map_pages %d\n", rc);
             goto out;
+	}
     }
 
     if ( ioreq_gfn )
@@ -805,12 +844,16 @@ int ioreq_server_get_frame(struct domain *d, ioservid_t id,
         goto out;
 
     rc = -EPERM;
-    if ( s->emulator != current->domain )
+    if ( s->emulator != current->domain ) {
+	printk("s->emulator5\n");
         goto out;
+    }
 
     rc = ioreq_server_alloc_pages(s);
-    if ( rc )
+    if ( rc ) {
+	printk("rc\n");
         goto out;
+    }
 
     switch ( idx )
     {
@@ -847,8 +890,12 @@ static int ioreq_server_map_io_range(struct domain *d, ioservid_t id,
     struct rangeset *r;
     int rc;
 
-    if ( start > end )
+    printk("ioreq_server_map_io_range\n");
+
+    if ( start > end ) {
+	    printk("start > end\n");
         return -EINVAL;
+    }
 
     rspin_lock(&d->ioreq_server.lock);
 
@@ -859,8 +906,10 @@ static int ioreq_server_map_io_range(struct domain *d, ioservid_t id,
         goto out;
 
     rc = -EPERM;
-    if ( s->emulator != current->domain )
+    if ( s->emulator != current->domain ) {
+	printk("s->emulator4\n");
         goto out;
+    }
 
     switch ( type )
     {
@@ -876,8 +925,10 @@ static int ioreq_server_map_io_range(struct domain *d, ioservid_t id,
     }
 
     rc = -EINVAL;
-    if ( !r )
+    if ( !r ) {
+	    printk("!r\n");
         goto out;
+    }
 
     rc = -EEXIST;
     if ( rangeset_overlaps_range(r, start, end) )
@@ -888,6 +939,7 @@ static int ioreq_server_map_io_range(struct domain *d, ioservid_t id,
  out:
     rspin_unlock(&d->ioreq_server.lock);
 
+    printk("ioreq_server_map_io_range %d\n", rc);
     return rc;
 }
 
@@ -911,8 +963,10 @@ static int ioreq_server_unmap_io_range(struct domain *d, ioservid_t id,
         goto out;
 
     rc = -EPERM;
-    if ( s->emulator != current->domain )
+    if ( s->emulator != current->domain ) {
+	printk("s->emulator3\n");
         goto out;
+    }
 
     switch ( type )
     {
@@ -972,8 +1026,10 @@ int ioreq_server_map_mem_type(struct domain *d, ioservid_t id,
         goto out;
 
     rc = -EPERM;
-    if ( s->emulator != current->domain )
+    if ( s->emulator != current->domain ) {
+	printk("s->emulator2\n");
         goto out;
+    }
 
     rc = arch_ioreq_server_map_mem_type(d, s, flags);
 
@@ -1001,8 +1057,10 @@ static int ioreq_server_set_state(struct domain *d, ioservid_t id,
         goto out;
 
     rc = -EPERM;
-    if ( s->emulator != current->domain )
+    if ( s->emulator != current->domain ) {
+	printk("s->emulator\n");
         goto out;
+    }
 
     domain_pause(d);
 
@@ -1289,6 +1347,7 @@ int ioreq_send(struct ioreq_server *s, ioreq_t *proto_p,
             }
 
             proto_p->state = STATE_IOREQ_NONE;
+	    printk("proto_p->vp_eport = %d\n", port);
             proto_p->vp_eport = port;
             *p = *proto_p;
 
@@ -1340,6 +1399,8 @@ int ioreq_server_dm_op(struct xen_dm_op *op, struct domain *d, bool *const_op)
 {
     long rc;
 
+    printk("ioreq_server_dm_op %d\n", op->op);
+
     switch ( op->op )
     {
     case XEN_DMOP_create_ioreq_server:
@@ -1353,6 +1414,7 @@ int ioreq_server_dm_op(struct xen_dm_op *op, struct domain *d, bool *const_op)
         if ( data->pad[0] || data->pad[1] || data->pad[2] )
             break;
 
+	printk("ioreq_server_create\n");
         rc = ioreq_server_create(d, data->handle_bufioreq,
                                  &data->id);
         break;
@@ -1364,11 +1426,14 @@ int ioreq_server_dm_op(struct xen_dm_op *op, struct domain *d, bool *const_op)
             &op->u.get_ioreq_server_info;
         const uint16_t valid_flags = XEN_DMOP_no_gfns;
 
+		printk("XEN_DMOP_get_ioreq_server_info\n");
+
         *const_op = false;
 
         rc = -EINVAL;
-        if ( data->flags & ~valid_flags )
+        if ( data->flags & ~valid_flags ) {
             break;
+	}
 
         rc = ioreq_server_get_info(d, data->id,
                                    (data->flags & XEN_DMOP_no_gfns) ?
diff --git a/xen/common/memory.c b/xen/common/memory.c
index 8ca4e1a842..2ac7351384 100644
--- a/xen/common/memory.c
+++ b/xen/common/memory.c
@@ -1186,6 +1186,8 @@ static int acquire_ioreq_server(struct domain *d,
         if ( rc )
             return rc;
 
+	printk("mfn_list[%d] = %lx %lx\n", i, mfn_x(mfn), mfn);
+
         mfn_list[i] = mfn_x(mfn);
     }
 
@@ -1292,6 +1294,7 @@ static int acquire_resource(
 
     if ( guest_handle_is_null(xmar.frame_list) )
     {
+	    printk("guest_handle_is_null(xmar.frame_list)");
         if ( xmar.nr_frames || start_extent )
             goto out;
 
@@ -1323,6 +1326,7 @@ static int acquire_resource(
         xen_pfn_t mfn_list[32];
         unsigned int todo = MIN(ARRAY_SIZE(mfn_list), xmar.nr_frames), done;
 
+	printk("_acquire_resource\n");
         rc = _acquire_resource(d, xmar.type, xmar.id, xmar.frame,
                                todo, mfn_list);
         if ( rc < 0 )
@@ -1340,6 +1344,7 @@ static int acquire_resource(
         /* Adjust guest frame_list appropriately. */
         if ( !paging_mode_translate(currd) )
         {
+		printk("copy_to_guest mfn_list\n");
             if ( copy_to_guest(xmar.frame_list, mfn_list, done) )
                 rc = -EFAULT;
         }
@@ -1348,11 +1353,13 @@ static int acquire_resource(
             xen_pfn_t gfn_list[ARRAY_SIZE(mfn_list)];
             unsigned int i;
 
+		printk("copy_from_guest gfn_list\n");
             if ( copy_from_guest(gfn_list, xmar.frame_list, done) )
                 rc = -EFAULT;
 
             for ( i = 0; !rc && i < done; i++ )
             {
+		printk("set_foreign_p2m_entry(currd = %p, d = %p, gfn_list[%d] = %lx, mfn_list[%d] = %lx)\n", currd, d, i, gfn_list[i], i, _mfn(mfn_list[i]));
                 rc = set_foreign_p2m_entry(currd, d, gfn_list[i],
                                            _mfn(mfn_list[i]));
                 /* rc should be -EIO for any iteration other than the first */
@@ -1364,11 +1371,15 @@ static int acquire_resource(
         if ( rc )
             goto out;
 
+	printk("done = %u\n", done);
+
         xmar.nr_frames -= done;
         xmar.frame += done;
         guest_handle_add_offset(xmar.frame_list, done);
         start_extent += done;
 
+	printk("xmar.frame_list.p %p\n", xmar.frame_list.p);
+
         /*
          * Explicit continuation request from _acquire_resource(), or we've
          * still got more work to do.
@@ -1849,9 +1860,44 @@ long do_memory_op(unsigned long cmd, XEN_GUEST_HANDLE_PARAM(void) arg)
 #endif
 
     case XENMEM_acquire_resource:
+#if 0
+        struct xen_mem_acquire_resource _ar;
+        XEN_GUEST_HANDLE_PARAM(xen_mem_acquire_resource_t) ar =
+            guest_handle_cast(arg, xen_mem_acquire_resource_t);
+
+        /* まず引数をコピー */
+        copy_from_guest(&_ar, ar, sizeof(_ar));
+
+        /* 事前ログ */
+        printk("HV_DBG: acquire_resource IN: domid=%u type=%u id=%u nr_frames=%u frame=0x%llx buf=%p\n",
+                   _ar.domid, _ar.type, _ar.id,
+                   _ar.nr_frames, (unsigned long long)_ar.frame,
+                   (void *)_ar.frame_list.p);
+        /* IN 側の buffer 中身 (未初期化ならガベージでも見る) */
+        if ( _ar.frame_list.p && _ar.nr_frames )
+        {
+            xen_pfn_t temp[16];
+            unsigned i, to = min(_ar.nr_frames, 16u);
+            copy_from_guest(temp, _ar.frame_list, to * sizeof(xen_pfn_t));
+            for ( i = 0; i < to; i++ )
+                printk("HV_DBG:   before frame_list[%u] = 0x%llx\n", i, (unsigned long long)temp[i]);
+        }
+#endif
         rc = acquire_resource(
             guest_handle_cast(arg, xen_mem_acquire_resource_t),
             start_extent);
+#if 0
+        /* 事後ログ：OUT 側 */
+        copy_from_guest(&_ar, ar, sizeof(_ar));
+        if ( _ar.frame_list.p && _ar.nr_frames )
+        {
+            xen_pfn_t temp[16];
+            unsigned i, to = min(_ar.nr_frames, 16u);
+            copy_from_guest(temp, _ar.frame_list, to * sizeof(xen_pfn_t));
+            for ( i = 0; i < to; i++ )
+                printk("HV_DBG:   after  frame_list[%u] = 0x%llx\n", i, (unsigned long long)temp[i]);
+        }
+#endif
         break;
 
     default:
@@ -1958,6 +2004,8 @@ int prepare_ring_for_helper(
     void *va;
     int rc;
 
+    printk("prepare_ring_for_helper\n");
+
     rc = check_get_page_from_gfn(d, _gfn(gmfn), false, &p2mt, &page);
     if ( rc )
         return (rc == -EAGAIN) ? -ENOENT : rc;
diff --git a/xen/include/xsm/xsm.h b/xen/include/xsm/xsm.h
index 4dbff9d866..b9d6b4525d 100644
--- a/xen/include/xsm/xsm.h
+++ b/xen/include/xsm/xsm.h
@@ -737,6 +737,7 @@ static inline int xsm_pmu_op(
 
 static inline int xsm_dm_op(xsm_default_t def, struct domain *d)
 {
+	printk("alternative_call(xsm_ops.dm_op\n");
     return alternative_call(xsm_ops.dm_op, d);
 }
 
diff --git a/xen/xsm/xsm_core.c b/xen/xsm/xsm_core.c
index f255fb63bf..e0540743c6 100644
--- a/xen/xsm/xsm_core.c
+++ b/xen/xsm/xsm_core.c
@@ -81,6 +81,8 @@ static int __init xsm_core_init(const void *policy_buffer, size_t policy_size)
 {
     const struct xsm_ops *ops = NULL;
 
+    printk("xsm_core_init\n");
+
 #ifdef CONFIG_XSM_FLASK_POLICY
     if ( policy_size == 0 )
     {
@@ -91,6 +93,7 @@ static int __init xsm_core_init(const void *policy_buffer, size_t policy_size)
 
     if ( xsm_ops_registered != XSM_OPS_UNREGISTERED )
     {
+    printk("!UNREGISTERED\n");
         printk(XENLOG_ERR
                "Could not init XSM, xsm_ops register already attempted\n");
         return -EIO;
@@ -99,18 +102,22 @@ static int __init xsm_core_init(const void *policy_buffer, size_t policy_size)
     switch ( xsm_bootparam )
     {
     case XSM_BOOTPARAM_DUMMY:
+    printk("dummy\n");
         xsm_ops_registered = XSM_OPS_REGISTERED;
         break;
 
     case XSM_BOOTPARAM_FLASK:
+    printk("flask\n");
         ops = flask_init(policy_buffer, policy_size);
         break;
 
     case XSM_BOOTPARAM_SILO:
+    printk("silo\n");
         ops = silo_init();
         break;
 
     default:
+    printk("default\n");
         ASSERT_UNREACHABLE();
         break;
     }
-- 
2.43.0

